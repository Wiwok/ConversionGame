import chalk from 'chalk';
import promptSync from 'prompt-sync';
const prompt = promptSync();


function BinDec() {

	// Generate a random bin value and its dec number

	// Tell the user the bin, and ask for the dec number

}


export default BinDec;