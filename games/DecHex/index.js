import chalk from 'chalk';
import promptSync from 'prompt-sync';
const prompt = promptSync();


function DecHex() {

	// Generate a random dec number and its hex value

	// Tell the user the dec, and ask for the hex value

}


export default DecHex;