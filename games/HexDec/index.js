import chalk from 'chalk';
import promptSync from 'prompt-sync';
const prompt = promptSync();


function HexDec() {

	// Generate a random hex value and its dec number

	// Tell the user the hex, and ask for the dec number

}


export default HexDec;